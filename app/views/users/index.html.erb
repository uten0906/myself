<% @page_title = "ユーザー一覧" %>
<h1 class="text-center"><%= @page_title %></h1>

<%= form_tag :search_users, method: :get, class: "search d-flex flex-column flex-md-row justify-content-center" do %>
  <%= text_field_tag "q", params[:q], class: "form-control col-md-5 mx-md-3" %>
  <%= submit_tag "検索", class: "btn btn-primary flex-fill flex-md-grow-0 my-3 my-md-0" %>
<% end %>

<div class="mx-auto col-12 col-lg-10">
  <% if @users.present? %>
    <%= render "info" %>
  <% else %>
    <p>ユーザー情報がありません</p>
  <% end %>
</div>
