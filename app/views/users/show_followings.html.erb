<% @page_title = @user.name + "さんのフォロー" %>

<h1 class="text-center"><%= @page_title %></h1>

<div class="mx-auto col-12 col-lg-10">
  <% if @users.present? %>
    <%= render "info" %>
  <% else %>
    <p>フォローしてません</p>
  <% end %>
</div>
